/* Local placeholder for Bootstrap Bundle JS (includes Popper) to allow offline operation.
   Replace with real bootstrap.bundle.min.js when available. */
(function(){
  // Minimal no-op to avoid runtime errors when data-bs-* attributes are present
  window.bootstrap = window.bootstrap || {};
})();
